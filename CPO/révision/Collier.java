public class Collier extends Bijou {
    private int nombredePerles;

    public Collier(int nombredePerles) {
        super(nombredePerles);
       this.nombredePerles = nombredePerles;
    }
    
}
